frappe.listview_settings['Tilda Webhook Configuration'] = {
    hide_name_column: true,
    hide_name_filter: true,
    onload: function(listview) {
         // Код можно убрать, если onload не нужен
    }
}; 